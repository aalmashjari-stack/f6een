import type { Level } from './types'

/**
 * صفوف لوح الجولة الجماعية — **موضعٌ واحد يقرؤه المحرّك كلُّه**.
 *
 * كانت القائمة مكتوبةً في أربعة مواضع: `session.ts` للعرض، و`bank.ts`
 * لصلاحية التصنيف، و`draw.ts` للتنازل، والاختبار. فلمّا دخل «تعجيزي» في
 * ٩ سبتمبر ٢٠٢٦ تبدّل موضعٌ وبقيت الثلاثة على ثلاثة — فصارت `playableCategories`
 * تُجيز تصنيفاً صفُّه الرابع فارغ، ويسقط السحبُ إلى تصنيفٍ آخر، فيظهر سؤال
 * تمثيلٍ في خليّة «أحياء وفلك» (بلاغ علي في اليوم نفسه).
 *
 * فمن زاد صفّاً يزيده هنا وحده.
 */
export const BOARD_LEVELS: Level[] = ['سهل', 'متوسط', 'صعب', 'تعجيزي']
