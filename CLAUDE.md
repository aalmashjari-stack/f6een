# فطين

لعبة أسئلة عربية جماعية تُلعب على شاشة واحدة كبيرة، يشغّلها شخص واحد كحكم.

## قبل أي عمل
اقرأ `docs/SPEC.md` كاملاً. هو مصدر الحقيقة الوحيد لقواعد اللعبة والشاشات.

## قواعد ثابتة
- الواجهة عربية RTL بالكامل.
- بنك الأسئلة في `data/questions-bank-v5.json` — قابل للقراءة والتعديل (قرار علي ٤ سبتمبر ٢٠٢٦؛ كان للقراءة فقط قبله). وأي تعديل فيه يبقى محكوماً بـ`bank.test.ts`: لكل خليّة (تصنيف × مستوى) نصّية عشرون سؤالاً على الأقل، والمعرّفات والنصوص بلا تكرار.
- **ومرجعُ الأسئلة في اللعب صار القاعدة لا الملفّ** (قرار علي ٥ سبتمبر ٢٠٢٦، `20260905120000_bank_in_db.sql`). الملفّ بذرةُ أوّل إقلاع ومرجعُ الاختبارات؛ وما يراه اللاعب هو `question_overrides` حين يكون مفتاح `bank_in_db` مرفوعاً. فتعديلُ الملفّ وحده لا يغيّر شيئاً في يد لاعب — التغييرُ من اللوحة، أو من الملفّ ثمّ رفعُه بـ«رفع ملفّ».
- الشعار والأيقونة في `assets/` — معتمدة نهائياً، لا تعيد تصميمها.
- لا تفترض شيئاً ورد في قسم "غير محسوم" في SPEC.md — اسألني.

## الاختبارات
`npm test` قبل أي commit يمسّ `src/game/`.

تحرس ضمانتين لا يكشفهما التصفّح اليدوي — تظهران بعد عشرين سؤالاً في جلسة
كاملة، لا في أول شاشة:
- لا سؤال يظهر مرّتين في الجلسة الواحدة.
- لا قالبان من عائلة واحدة («ما العنصر الذي رمزه…؟») في الجلسة الواحدة.

و`session-flow.test.ts` يقود جلسة كاملة عبر الـ reducer نفسه لا عبر محاكاة
تعيد كتابة منطقه — فتعديلٌ في المحرك يظهر أثره في الاختبار فوراً.
