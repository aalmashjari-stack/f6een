node_modules/
dist/
.DS_Store
*.local
.vite/
# ناتج بناء لا مصدر — يتغيّر مع كل `npm run build` فيلوّث كل commit بسطر لا معنى له
tsconfig.tsbuildinfo

# أصول صور التصنيفات كما وصلت (١٠٢٤px، ١٢ ميجابايت) — نسخها المصغّرة المستخدمة
# في التطبيق داخل assets/categories/ وهي وحدها ما يحتاجه البناء.
# الشرطة المائلة في الأول تقيّده بجذر المستودع: git على macOS لا يفرّق بين
# حالتَي الحرف، فالنمط المطلق يبتلع assets/categories/ ويكسر البناء.
/Categories/

# design-sync
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
